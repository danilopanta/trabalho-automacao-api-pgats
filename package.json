{
  "name": "hotel-reservas-api",
  "version": "1.0.0",
  "description": "API REST para gerenciamento de reservas de hotel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "mocha 'test/**/*.js' --timeout 10000 --reporter mochawesome",
    "test-rest-external": "mocha \"test/rest/external/**/*.js\" --timeout 10000 --reporter mochawesome",
    "test-rest-controller": "mocha \"test/rest/controller/**/*.js\" --timeout 10000",
    "test-rest": "mocha \"test/rest/**/*.js\" --timeout 10000 --reporter mochawesome"
  },
  "keywords": [
    "api",
    "rest",
    "express",
    "hotel",
    "reservas",
    "swagger"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "chai-exclude": "^2.1.0",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.4",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  }
}
