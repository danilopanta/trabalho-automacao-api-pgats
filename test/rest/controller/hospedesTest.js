const request = require('supertest');
const { expect } = require('chai');

// Note: Para os testes REST funcionarem, seria necessário iniciar o servidor
// Por enquanto, mantemos a estrutura básica

describe('Testes REST - Hóspedes', () => {

    it('Validar que é possível listar hóspedes via REST', async () => {
        // Teste estrutural - seria implementado quando o servidor REST estiver rodando
        expect(true).to.be.true;
    });

    it('Validar estrutura de resposta REST para hóspedes', async () => {
        // Teste estrutural - seria implementado quando o servidor REST estiver rodando
        expect(true).to.be.true;
    });

});
