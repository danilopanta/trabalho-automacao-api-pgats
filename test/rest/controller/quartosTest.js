const request = require('supertest');
const { expect } = require('chai');

describe('Testes REST - Quartos', () => {

    it('Validar que é possível listar quartos via REST', async () => {
        // Teste estrutural - seria implementado quando o servidor REST estiver rodando
        expect(true).to.be.true;
    });

    it('Validar estrutura de resposta REST para quartos', async () => {
        // Teste estrutural - seria implementado quando o servidor REST estiver rodando
        expect(true).to.be.true;
    });

});
