const request = require('supertest');
const { expect } = require('chai');

describe('Testes REST - Reservas', () => {

    it('Validar que é possível listar reservas via REST', async () => {
        // Teste estrutural - seria implementado quando o servidor REST estiver rodando
        expect(true).to.be.true;
    });

    it('Validar estrutura de resposta REST para reservas', async () => {
        // Teste estrutural - seria implementado quando o servidor REST estiver rodando
        expect(true).to.be.true;
    });

});
